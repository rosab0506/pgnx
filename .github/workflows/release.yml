name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-linux-cross:
    strategy:
      matrix:
        include:
          - platform: linux
            arch: x64
            target: x86_64-linux-gnu
            cc: gcc
            cxx: g++
          - platform: linux
            arch: arm64
            target: aarch64-linux-gnu
            cc: aarch64-linux-gnu-gcc
            cxx: aarch64-linux-gnu-g++
          - platform: win32
            arch: x64
            target: x86_64-w64-mingw32
            cc: x86_64-w64-mingw32-gcc
            cxx: x86_64-w64-mingw32-g++

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'

    - name: Install Python and Conan
      run: |
        sudo apt-get update
        sudo apt-get install -y python3 python3-pip pipx
        pipx install conan
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Install cross-compilation toolchains
      run: |
        sudo apt-get install -y build-essential libssl-dev wget tar zlib1g-dev
        if [ "${{ matrix.arch }}" = "arm64" ]; then
          sudo dpkg --add-architecture arm64
          echo "deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports noble main restricted universe multiverse" | sudo tee /etc/apt/sources.list.d/arm64.list
          echo "deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports noble-updates main restricted universe multiverse" | sudo tee -a /etc/apt/sources.list.d/arm64.list
          echo "deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports noble-security main restricted universe multiverse" | sudo tee -a /etc/apt/sources.list.d/arm64.list
          sudo sed -i 's/^deb \[arch=amd64,i386\]/deb [arch=amd64,i386]/' /etc/apt/sources.list
          sudo sed -i '/security\.ubuntu\.com.*arm64/d' /etc/apt/sources.list
          sudo apt-get update || true
          sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu \
            crossbuild-essential-arm64 zlib1g-dev:arm64 libssl-dev:arm64
        fi
        if [ "${{ matrix.platform }}" = "win32" ]; then
          sudo apt-get install -y mingw-w64
        fi

    - name: Setup dependencies with Conan
      run: |
        chmod +x $GITHUB_WORKSPACE/scripts/conan-setup.sh
        bash $GITHUB_WORKSPACE/scripts/conan-setup.sh ${{ matrix.platform }} ${{ matrix.arch }}

    - name: Create binding.gyp (Linux)
      if: matrix.platform == 'linux'
      run: |
        cat > $GITHUB_WORKSPACE/binding.gyp << 'GYEOF'
        {
          "targets": [{
            "target_name": "pgnx",
            "sources": ["src/addon.cpp", "src/connection_pool.cpp", "src/connection.cpp", "src/listener.cpp"],
            "include_dirs": [
              "<!@(node -p \"require('node-addon-api').include\")",
              "/tmp/pgsql/include"
            ],
            "cflags!": ["-fno-exceptions", "-fno-rtti"],
            "cflags_cc!": ["-fno-exceptions", "-fno-rtti"],
            "cflags_cc": ["-std=c++17", "-fexceptions", "-frtti", "-O3"],
            "defines": ["NAPI_VERSION=8"],
            "libraries": [
              "/tmp/pgsql/lib/libpqxx.a",
              "/tmp/pgsql/lib/libpq.a",
              "/tmp/postgresql-16.1/src/common/libpgcommon.a",
              "/tmp/postgresql-16.1/src/port/libpgport.a",
              "-lssl", "-lcrypto", "-lz", "-lpthread"
            ]
          }]
        }
        GYEOF

    - name: Create binding.gyp (Windows)
      if: matrix.platform == 'win32'
      run: |
        cat > $GITHUB_WORKSPACE/binding.gyp << 'GYEOF'
        {
          "targets": [{
            "target_name": "pgnx",
            "sources": ["src/addon.cpp", "src/connection_pool.cpp", "src/connection.cpp", "src/listener.cpp"],
            "include_dirs": [
              "<!@(node -p \"require('node-addon-api').include\")",
              "/tmp/pgsql/include"
            ],
            "cflags!": ["-fno-exceptions", "-fno-rtti"],
            "cflags_cc!": ["-fno-exceptions", "-fno-rtti"],
            "cflags_cc": ["-std=c++17", "-fexceptions", "-frtti", "-O3"],
            "defines": ["NAPI_VERSION=8", "WIN32"],
            "libraries": [
              "/tmp/pgsql/lib/libpqxx.a",
              "/tmp/pgsql/lib/libpq.a",
              "/tmp/postgresql-16.1/src/common/libpgcommon.a",
              "/tmp/postgresql-16.1/src/port/libpgport.a",
              "/tmp/node.lib",
              "-lws2_32", "-lsecur32", "-lcrypt32", "-lwldap32"
            ],
            "ldflags!": ["-rdynamic"],
            "ldflags": ["-static-libgcc", "-static-libstdc++"]
          }]
        }
        GYEOF

    - name: Install npm dependencies
      run: npm install --ignore-scripts

    - name: Build native addon (Linux x64)
      if: matrix.platform == 'linux' && matrix.arch == 'x64'
      run: npx node-gyp rebuild

    - name: Build native addon (Linux ARM64)
      if: matrix.platform == 'linux' && matrix.arch == 'arm64'
      run: |
        export CC=aarch64-linux-gnu-gcc
        export CXX=aarch64-linux-gnu-g++
        export AR=aarch64-linux-gnu-ar
        npx node-gyp rebuild --arch=arm64

    - name: Build native addon (Windows x64)
      if: matrix.platform == 'win32'
      run: |
        export CC=x86_64-w64-mingw32-gcc
        export CXX=x86_64-w64-mingw32-g++
        export AR=x86_64-w64-mingw32-ar
        export LINK=x86_64-w64-mingw32-g++
        export LDFLAGS="-static-libgcc -static-libstdc++"
        npx node-gyp install
        NODE_VERSION=$(node -v | sed 's/v//')
        wget -q https://nodejs.org/download/release/v${NODE_VERSION}/win-x64/node.lib -O /tmp/node.lib
        npx node-gyp rebuild --arch=x64

    - name: Create prebuild directory structure
      run: |
        mkdir -p prebuilds/${{ matrix.platform }}-${{ matrix.arch }}
        cp build/Release/pgnx.node prebuilds/${{ matrix.platform }}-${{ matrix.arch }}/pgnx.node

    - name: Upload prebuild artifact
      uses: actions/upload-artifact@v4
      with:
        name: prebuild-${{ matrix.platform }}-${{ matrix.arch }}
        path: prebuilds/
        retention-days: 1

  build-macos:
    strategy:
      matrix:
        include:
          - os: macos-13
            arch: x64
          - os: macos-14
            arch: arm64

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'

    - name: Install Python and Conan
      run: |
        brew install pipx
        pipx install conan
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Install dependencies
      run: |
        brew install openssl@3

    - name: Setup Conan profile
      run: |
        conan profile detect --force

    - name: Install Conan dependencies
      run: |
        mkdir -p build-conan
        cd build-conan
        conan install $GITHUB_WORKSPACE \
          --build=missing \
          -s build_type=Release \
          -s compiler.cppstd=17 \
          -o "*:shared=False"

    - name: Build PostgreSQL
      run: |
        cd /tmp
        wget -q https://ftp.postgresql.org/pub/source/v16.1/postgresql-16.1.tar.gz
        tar xzf postgresql-16.1.tar.gz
        cd postgresql-16.1
        CFLAGS="-fPIC -O3" ./configure \
          --prefix=/tmp/pgsql \
          --without-readline \
          --without-icu \
          --with-openssl \
          --with-includes=$(brew --prefix openssl@3)/include \
          --with-libraries=$(brew --prefix openssl@3)/lib
        make -C src/interfaces/libpq -j$(sysctl -n hw.ncpu)
        make -C src/interfaces/libpq install
        make -C src/bin/pg_config install
        make -C src/include install
        make -C src/common -j$(sysctl -n hw.ncpu)
        make -C src/port -j$(sysctl -n hw.ncpu)

    - name: Build libpqxx
      run: |
        cd /tmp
        wget -q https://github.com/jtv/libpqxx/archive/refs/tags/7.8.1.tar.gz
        tar xzf 7.8.1.tar.gz
        cd libpqxx-7.8.1
        CXX="clang++ -std=c++17" CXXFLAGS="-fPIC -O3" ./configure \
          --prefix=/tmp/pgsql \
          --enable-shared=no \
          --with-postgres-include=/tmp/pgsql/include \
          --with-postgres-lib=/tmp/pgsql/lib
        make -j$(sysctl -n hw.ncpu)
        make install

    - name: Create binding.gyp
      run: |
        OPENSSL_PREFIX=$(brew --prefix openssl@3)
        cat > $GITHUB_WORKSPACE/binding.gyp << GYEOF
        {
          "targets": [{
            "target_name": "pgnx",
            "sources": ["src/addon.cpp", "src/connection_pool.cpp", "src/connection.cpp", "src/listener.cpp"],
            "include_dirs": [
              "<!@(node -p \\"require('node-addon-api').include\\")",
              "/tmp/pgsql/include"
            ],
            "cflags!": ["-fno-exceptions", "-fno-rtti"],
            "cflags_cc!": ["-fno-exceptions", "-fno-rtti"],
            "cflags_cc": ["-std=c++17", "-fexceptions", "-frtti", "-O3"],
            "defines": ["NAPI_VERSION=8"],
            "libraries": [
              "/tmp/pgsql/lib/libpqxx.a",
              "/tmp/pgsql/lib/libpq.a",
              "/tmp/postgresql-16.1/src/common/libpgcommon.a",
              "/tmp/postgresql-16.1/src/port/libpgport.a",
              "${OPENSSL_PREFIX}/lib/libssl.a",
              "${OPENSSL_PREFIX}/lib/libcrypto.a",
              "-lz"
            ],
            "xcode_settings": {
              "GCC_ENABLE_CPP_EXCEPTIONS": "YES",
              "GCC_ENABLE_CPP_RTTI": "YES",
              "CLANG_CXX_LANGUAGE_STANDARD": "c++17",
              "MACOSX_DEPLOYMENT_TARGET": "10.15",
              "OTHER_LDFLAGS": [
                "-framework", "CoreFoundation",
                "-framework", "Security"
              ],
              "OTHER_CFLAGS": ["-I${OPENSSL_PREFIX}/include"],
              "OTHER_CPLUSPLUSFLAGS": ["-I${OPENSSL_PREFIX}/include"]
            }
          }]
        }
        GYEOF

    - name: Install npm dependencies
      run: npm install --ignore-scripts

    - name: Build native addon
      run: npx node-gyp rebuild

    - name: Create prebuild directory structure
      run: |
        mkdir -p prebuilds/darwin-${{ matrix.arch }}
        cp build/Release/pgnx.node prebuilds/darwin-${{ matrix.arch }}/pgnx.node

    - name: Upload prebuild artifact
      uses: actions/upload-artifact@v4
      with:
        name: prebuild-darwin-${{ matrix.arch }}
        path: prebuilds/
        retention-days: 1

  publish:
    needs: [build-linux-cross, build-macos]
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        registry-url: 'https://registry.npmjs.org'

    - name: Download all prebuilds
      uses: actions/download-artifact@v4
      with:
        path: prebuilds-temp

    - name: Merge prebuilds into package
      run: |
        mkdir -p prebuilds
        for dir in prebuilds-temp/prebuild-*/prebuilds/*; do
          if [ -d "$dir" ]; then
            platform_arch=$(basename "$dir")
            mkdir -p "prebuilds/$platform_arch"
            cp -r "$dir"/* "prebuilds/$platform_arch/"
          fi
        done
        echo "Prebuilds ready:"
        ls -lhR prebuilds/

    - name: Publish to npm
      run: npm publish --access public
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
